<!-- Spectra Sound — Bundles Page -->
<div class="bun-page">

  <div class="bun-glow"></div>

  <!-- ── Header ── -->
  <div class="bun-header">
    <p class="bun-eyebrow">Spectra Sound</p>
    <h1 class="bun-title">Bundles</h1>
    <p class="bun-desc">Get more for less. Each bundle combines multiple packs into one complete collection — the smartest way to build your sound library.</p>
    <div class="bun-header__meta">
      <span class="bun-header__tag">
        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#c9a84c" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
        Commercial license included
      </span>
      <span class="bun-header__tag">
        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#c9a84c" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
        Instant download
      </span>
      <span class="bun-header__tag">
        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#c9a84c" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
        Royalty-free forever
      </span>
    </div>
  </div>

  <!-- ── Bundle Grid ── -->
  <div class="bun-grid">

    <!-- Drone Fly Bys Essentials Bundle -->
    <a href="/products/drone-fly-bys-essentials-bundle" class="bun-card">
      <div class="bun-card__img-wrap">
        <div class="bun-card__img bun-card__img--essentials"></div>
        <div class="bun-card__overlay">
          <span class="bun-card__cta">View Bundle →</span>
        </div>
        <div class="bun-card__badge">Bundle</div>
        <div class="bun-card__pack-count">5 Packs Included</div>
      </div>
      <div class="bun-card__body">
        <div class="bun-card__left">
          <p class="bun-card__category">Drone Fly Bys</p>
          <h2 class="bun-card__name">Essentials Bundle</h2>
          <p class="bun-card__detail">The five most essential Drone Fly Bys environments in a single download. Forest & Trees, Ocean & Waves, Rocks & Cliffs, Wind, and Rivers & Lakes — everything you need to cover the most common aerial footage scenes.</p>
          <div class="bun-card__includes">
            <span class="bun-card__includes-label">Includes</span>
            <div class="bun-card__tags">
              <span>Forest & Trees</span>
              <span>Ocean & Waves</span>
              <span>Rocks & Cliffs</span>
              <span>Wind</span>
              <span>Rivers & Lakes</span>
            </div>
          </div>
        </div>
        <div class="bun-card__right">
          <div class="bun-card__pricing">
            <span class="bun-card__price">From $59</span>
            <span class="bun-card__saving">Save 35%</span>
          </div>
          <span class="bun-card__btn">View Bundle</span>
        </div>
      </div>
    </a>

    <!-- Complete Collection — Coming Soon -->
    <div class="bun-card bun-card--coming">
      <div class="bun-card__img-wrap">
        <div class="bun-card__img bun-card__img--complete"></div>
        <div class="bun-card__badge bun-card__badge--soon">Coming Soon</div>
        <div class="bun-card__pack-count bun-card__pack-count--soon">10 Packs</div>
      </div>
      <div class="bun-card__body">
        <div class="bun-card__left">
          <p class="bun-card__category">Drone Fly Bys</p>
          <h2 class="bun-card__name">Complete Collection</h2>
          <p class="bun-card__detail">Every single Drone Fly Bys pack in one complete bundle — all 10 environments, hundreds of sounds, total coverage for every aerial scenario imaginable. The ultimate library for serious drone creators.</p>
          <div class="bun-card__includes">
            <span class="bun-card__includes-label">Will include</span>
            <div class="bun-card__tags bun-card__tags--muted">
              <span>All 10 Environments</span>
              <span>Forest & Trees</span>
              <span>Ocean & Waves</span>
              <span>Rocks & Cliffs</span>
              <span>Wind</span>
              <span>+ 5 more</span>
            </div>
          </div>
        </div>
        <div class="bun-card__right">
          <div class="bun-card__pricing">
            <span class="bun-card__price bun-card__price--tba">TBA</span>
            <span class="bun-card__saving bun-card__saving--muted">Best value</span>
          </div>
          <span class="bun-card__btn bun-card__btn--muted">Coming Soon</span>
        </div>
      </div>
    </div>

  </div>

  <!-- ── Why Bundle ── -->
  <div class="bun-why">
    <div class="bun-why__line"></div>
    <div class="bun-why__inner">
      <p class="bun-eyebrow">Why Bundle?</p>
      <div class="bun-why__grid">
        <div class="bun-why__item">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#c9a84c" stroke-width="1.4"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>
          <h3>Better value</h3>
          <p>Bundles are priced well below buying each pack individually — the more you need, the more you save.</p>
        </div>
        <div class="bun-why__item">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#c9a84c" stroke-width="1.4"><path d="M21 16V8a2 2 0 00-1-1.73l-7-4a2 2 0 00-2 0l-7 4A2 2 0 003 8v8a2 2 0 001 1.73l7 4a2 2 0 002 0l7-4A2 2 0 0021 16z"/></svg>
          <h3>One download</h3>
          <p>Everything arrives in a single organised download — no managing multiple purchases or separate folders.</p>
        </div>
        <div class="bun-why__item">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#c9a84c" stroke-width="1.4"><path d="M22 11.08V12a10 10 0 11-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
          <h3>Full coverage</h3>
          <p>Never get caught without the right sound for a scene — bundles give you complete environmental coverage.</p>
        </div>
      </div>
    </div>
    <div class="bun-why__line"></div>
  </div>

</div>

<style>
  .bun-page {
    background: #0a0a0a;
    min-height: 100vh;
    padding-bottom: 0;
    position: relative;
    overflow-x: hidden;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  }

  .bun-glow {
    position: absolute;
    top: -80px;
    left: 50%;
    transform: translateX(-50%);
    width: 800px;
    height: 500px;
    background: radial-gradient(ellipse, rgba(201,168,76,0.07) 0%, transparent 65%);
    pointer-events: none;
    z-index: 0;
  }

  /* ── Header ── */
  .bun-header {
    max-width: 1100px;
    margin: 0 auto;
    padding: 5rem 2rem 3.5rem;
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .bun-eyebrow {
    font-size: 0.62rem;
    letter-spacing: 0.28em;
    text-transform: uppercase;
    color: #c9a84c;
    margin: 0 0 1.25rem;
  }

  .bun-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 900;
    color: #fff;
    letter-spacing: -0.04em;
    line-height: 1;
    margin: 0 0 1.25rem;
  }

  .bun-desc {
    font-size: 0.95rem;
    color: #4a4a4a;
    line-height: 1.75;
    margin: 0 auto 2rem;
    max-width: 520px;
  }

  .bun-header__meta {
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .bun-header__tag {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-size: 0.65rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #2e2e2e;
    border: 1px solid #161616;
    padding: 0.4rem 0.9rem;
    border-radius: 3px;
  }

  /* ── Grid ── */
  .bun-grid {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem 5rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    position: relative;
    z-index: 1;
  }

  /* ── Card ── */
  .bun-card {
    display: block;
    text-decoration: none;
    background: #0d0d0d;
    border: 1px solid #1a1a1a;
    border-radius: 10px;
    overflow: hidden;
    transition: border-color 0.3s, transform 0.3s;
  }

  .bun-card:hover {
    border-color: rgba(201,168,76,0.25);
    transform: translateY(-3px);
  }

  .bun-card--coming {
    pointer-events: none;
    opacity: 0.55;
  }

  /* Image area */
  .bun-card__img-wrap {
    position: relative;
    aspect-ratio: 21 / 7;
    overflow: hidden;
  }

  .bun-card__img {
    width: 100%;
    height: 100%;
    background-size: cover;
    background-position: center;
    transition: transform 0.5s ease;
  }

  .bun-card:hover .bun-card__img { transform: scale(1.03); }

  .bun-card__img--essentials {
    background: linear-gradient(135deg,
      #061008 0%,
      #0a1810 20%,
      #060e18 40%,
      #080810 60%,
      #0c1008 80%,
      #060c10 100%
    );
    position: relative;
  }

  .bun-card__img--essentials::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 20% 50%, rgba(10,40,20,0.6) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 50%, rgba(6,20,40,0.6) 0%, transparent 50%),
      radial-gradient(ellipse at 50% 50%, rgba(201,168,76,0.04) 0%, transparent 60%);
  }

  .bun-card__img--complete {
    background: linear-gradient(135deg, #0a0a0a 0%, #111 50%, #0a0a0a 100%);
  }

  /* Dark scrim */
  .bun-card__img::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(0,0,0,0.1) 0%, rgba(0,0,0,0.5) 100%);
    pointer-events: none;
  }

  /* Hover overlay */
  .bun-card__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s;
    z-index: 2;
  }

  .bun-card:hover .bun-card__overlay { opacity: 1; }

  .bun-card__cta {
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #0a0a0a;
    background: #c9a84c;
    padding: 0.65rem 1.5rem;
    border-radius: 3px;
  }

  /* Badge */
  .bun-card__badge {
    position: absolute;
    top: 1.25rem;
    left: 1.25rem;
    font-size: 0.58rem;
    font-weight: 800;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #0a0a0a;
    background: #c9a84c;
    padding: 0.3rem 0.75rem;
    border-radius: 2px;
    z-index: 2;
  }

  .bun-card__badge--soon {
    background: transparent;
    color: #333;
    border: 1px solid #222;
  }

  .bun-card__pack-count {
    position: absolute;
    top: 1.25rem;
    right: 1.25rem;
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.4);
    z-index: 2;
  }

  .bun-card__pack-count--soon { color: #222; }

  /* Body */
  .bun-card__body {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 2rem;
    padding: 1.75rem 2rem 2rem;
  }

  .bun-card__left { flex: 1; min-width: 0; }

  .bun-card__category {
    font-size: 0.6rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #c9a84c;
    margin: 0 0 0.4rem;
    font-weight: 600;
  }

  .bun-card__name {
    font-size: clamp(1.3rem, 2.5vw, 1.75rem);
    font-weight: 800;
    color: #fff;
    letter-spacing: -0.02em;
    margin: 0 0 0.85rem;
    line-height: 1.1;
  }

  .bun-card__detail {
    font-size: 0.83rem;
    color: #3e3e3e;
    line-height: 1.7;
    margin: 0 0 1.25rem;
    max-width: 540px;
  }

  .bun-card__includes { }

  .bun-card__includes-label {
    font-size: 0.58rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #222;
    display: block;
    margin-bottom: 0.6rem;
    font-weight: 600;
  }

  .bun-card__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .bun-card__tags span {
    font-size: 0.68rem;
    color: #333;
    border: 1px solid #161616;
    padding: 0.25rem 0.65rem;
    border-radius: 2px;
    letter-spacing: 0.04em;
  }

  .bun-card__tags--muted span { color: #1e1e1e; border-color: #111; }

  /* Right side */
  .bun-card__right {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 1rem;
    flex-shrink: 0;
    padding-top: 0.25rem;
  }

  .bun-card__pricing {
    text-align: right;
  }

  .bun-card__price {
    display: block;
    font-size: 1.75rem;
    font-weight: 900;
    color: #c9a84c;
    letter-spacing: -0.03em;
    line-height: 1;
    margin-bottom: 0.3rem;
  }

  .bun-card__price--tba {
    font-size: 1.1rem;
    color: #222;
    letter-spacing: 0.05em;
  }

  .bun-card__saving {
    display: inline-block;
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #0a0a0a;
    background: #c9a84c;
    padding: 0.22rem 0.6rem;
    border-radius: 2px;
  }

  .bun-card__saving--muted {
    background: transparent;
    color: #1e1e1e;
    border: 1px solid #141414;
  }

  .bun-card__btn {
    display: inline-flex;
    align-items: center;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #0a0a0a;
    background: #c9a84c;
    padding: 0.75rem 1.5rem;
    border-radius: 4px;
    white-space: nowrap;
    transition: background 0.2s;
  }

  .bun-card__btn--muted {
    background: transparent;
    color: #1e1e1e;
    border: 1px solid #141414;
  }

  /* ── Why Bundle ── */
  .bun-why {
    background: #080808;
    border-top: 1px solid #111;
    padding: 5rem 2rem 6rem;
    position: relative;
    z-index: 1;
  }

  .bun-why__line {
    display: none;
  }

  .bun-why__inner {
    max-width: 1100px;
    margin: 0 auto;
    text-align: center;
  }

  .bun-why__inner .bun-eyebrow {
    margin-bottom: 3rem;
  }

  .bun-why__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    text-align: left;
  }

  .bun-why__item {
    background: #0d0d0d;
    border: 1px solid #141414;
    border-radius: 8px;
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .bun-why__item h3 {
    font-size: 0.9rem;
    font-weight: 700;
    color: #ccc;
    letter-spacing: -0.01em;
    margin: 0;
  }

  .bun-why__item p {
    font-size: 0.8rem;
    color: #333;
    line-height: 1.7;
    margin: 0;
  }

  /* ── Mobile ── */
  @media (max-width: 860px) {
    .bun-card__body { flex-direction: column; gap: 1.5rem; }
    .bun-card__right { align-items: flex-start; flex-direction: row; align-items: center; }
    .bun-why__grid { grid-template-columns: 1fr; }
    .bun-card__img-wrap { aspect-ratio: 16 / 7; }
  }

  @media (max-width: 540px) {
    .bun-header { padding: 3.5rem 1.25rem 2.5rem; }
    .bun-grid { padding: 0 1.25rem 4rem; }
    .bun-card__body { padding: 1.25rem; }
    .bun-card__img-wrap { aspect-ratio: 16 / 8; }
  }
</style>
